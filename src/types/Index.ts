export default interface Index {
  marketName: string;
  openingPrice: string;
  highPrice: string;
  lowPrice: string;
  currentPrice: string;
  previousDayComparison: string;
  previousDayRatio: string;
  RealtimeGraphURL: string;
  HistoricalGraphURL: string;
}
